# ==========================================
# ADMIN DASHBOARD - PRODUCTION ENVIRONMENT VARIABLES
# ==========================================
# Copy this file and configure for your production environment
#
# IMPORTANT: Never commit actual production values to version control
# These values should be set in Vercel's Environment Variables settings

# ==========================================
# CLERK AUTHENTICATION - ADMIN DASHBOARD
# ==========================================
# Get these from: https://dashboard.clerk.com/
# Create a separate Clerk application for the Admin Dashboard
# This ensures admin and client authentication are completely isolated

# Public key (safe to expose in client-side code)
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_live_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# Secret key (MUST be kept secret, server-side only)
CLERK_SECRET_KEY=your_clerk_secret_key_here

# Webhook secret for syncing Clerk users to database
# Get from: Clerk Dashboard > Webhooks > Add Endpoint
# Webhook URL: https://your-admin-domain.vercel.app/api/webhooks/clerk
# Subscribe to: user.created, user.updated, user.deleted
CLERK_WEBHOOK_SECRET=whsec_XXXXXXXXXXXXXXXXXXXXXXXXXXXXXXXX

# ==========================================
# DATABASE CONNECTION
# ==========================================
# Production database URL
# Options:
# 1. Vercel Postgres: postgresql://user:password@host:port/database?sslmode=require
# 2. Supabase: postgresql://postgres:password@db.project.supabase.co:5432/postgres
# 3. PlanetScale: mysql://user:password@host/database?sslaccept=strict
# 4. Railway: postgresql://user:password@host:port/database
#
# IMPORTANT: Both apps should share the same database for data consistency
# Ensure connection pooling is enabled for production use
DATABASE_URL="postgresql://user:password@host:port/database?sslmode=require"

# ==========================================
# EMAIL SERVICE (RESEND)
# ==========================================
# Get your API key from: https://resend.com/api-keys
# Both apps can share the same Resend API key
RESEND_API_KEY=your_resend_api_key_here

# From email address (must be verified in Resend)
# Use your custom domain: admin@yourdomain.com
# Or Resend's test domain for testing: onboarding@resend.dev
FROM_EMAIL=admin@yourdomain.com

# From name (appears as sender name in email clients)
FROM_NAME=YourCompany Admin

# Reply-to email address (where replies will go)
REPLY_TO_EMAIL=admin@yourdomain.com

# ==========================================
# APPLICATION URLS
# ==========================================
# Public URL of the Admin Dashboard app
# Used in emails, webhooks, and redirects
# Production example: https://admin.yourdomain.com
APP_URL=https://admin.yourdomain.com
NEXT_PUBLIC_APP_URL=https://admin.yourdomain.com

# Client Portal URL (for invitation links and references)
# Production example: https://portal.yourdomain.com
CLIENT_PORTAL_URL=https://portal.yourdomain.com

# ==========================================
# OPTIONAL: DEVELOPMENT/TESTING OVERRIDES
# ==========================================
# Leave these empty in production
# Only use for testing in staging environments

# Override recipient email for all outgoing emails (staging/testing only)
# DEV_EMAIL_TO=

# Email address for test emails (staging/testing only)
# TEST_EMAIL_TO=

# ==========================================
# DEPLOYMENT NOTES
# ==========================================
# 1. Set all environment variables in Vercel Dashboard:
#    Project Settings > Environment Variables
#
# 2. Configure separate Clerk applications:
#    - One for Admin Dashboard (this app)
#    - One for Client Portal
#    This ensures proper user isolation and role management
#
# 3. Database migrations:
#    Run migrations before deploying:
#    npm run db:migrate:deploy
#
# 4. Email verification:
#    Verify your domain in Resend before sending production emails
#
# 5. Webhook endpoints:
#    Configure Clerk webhook to point to:
#    https://admin.yourdomain.com/api/webhooks/clerk
#
# 6. Security:
#    - Never commit this file with real values
#    - Use Vercel's secret environment variables
#    - Rotate secrets regularly
#    - Enable 2FA on all service accounts
#    - Restrict admin access to trusted IPs if possible
#
# 7. Role Management:
#    - Configure Clerk roles and permissions
#    - Set up organization hierarchy
#    - Define admin, manager, and accountant roles
